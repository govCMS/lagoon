# Add additional security headers to each requeest served
# by nginxn.

add_header X-XSS-Protection "${X_XSS_PROTECTIION:-1; mode=block}";
add_header X-Content-Type-Options "${X_CONTENT_TYPE_OPTIONS:-nosniff}"
